@page
@model AppManager.Pages.Account.LoginModel
@{
    ViewData["Title"] = "Anmeldung";
}

<h2>Anmeldung</h2>

@if (!string.IsNullOrEmpty(Model.Message))
{
    <div class="alert alert-info">@Model.Message</div>
}

<form method="post">
    <div class="form-group">
        <label asp-for="Input.Username">Benutzername oder E-Mail</label>
        <input asp-for="Input.Username" class="form-control" placeholder="z.B. gülistan oder ihre@email.de" />
        <span asp-validation-for="Input.Username" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="Input.Password"></label>
        <input asp-for="Input.Password" class="form-control" type="password" />
        <span asp-validation-for="Input.Password" class="text-danger"></span>
    </div>

    <button type="submit" class="btn btn-primary">Anmelden</button>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
